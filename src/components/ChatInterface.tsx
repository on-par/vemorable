/**
 * ChatInterface Component (Legacy Wrapper)
 * 
 * This file now serves as a wrapper to maintain backward compatibility
 * while using the refactored chat components from the features/chat directory.
 * 
 * All chat logic has been moved to the vertical slice architecture.
 */

'use client';

import { ChatInterface } from '@/features/chat';

export default ChatInterface;